# tmux prefix key is junk
set -g prefix C-a
unbind-key C-b
bind-key C-a send-prefix

set -g mouse on

setw -g aggressive-resize on
set-window-option -g aggressive-resize on

set -g default-command /bin/bash
set -g default-shell /bin/bash

# turn on utf8
set -g status-utf8 on

# set color
set -g default-terminal "screen-256color"

# pass through fancy key combos (C-Arrows/S-Arrows)
set-window-option -g xterm-keys on

# minimize delay
set -sg escape-time 100

# auto rename
set -g automatic-rename

# notifications
set-window-option -g monitor-activity on
setw -g monitor-activity on
setw -g visual-activity on

# force a reload of the config file
bind C-r source-file ~/.tmux.conf; display "Reloaded"

# vi-style controls in copy mode
set-option -g status-keys vi
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send-keys -X begin-selection
bind-key -T copy-mode-vi 'y' send-keys -X copy-selection
setw -g mode-keys vi
bind Escape copy-mode

# macOS (Darwin) specific settings
if-shell '[ "$(uname -s)" = "Darwin" ]' {
  # bind C-c run "tmux save-buffer - | pbcopy"
  # bind C-v run "tmux set-buffer \"$(pbpaste)\"; tmux paste-buffer"

  bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
  bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"
  bind-key ] run "pbpaste | tmux load-buffer - ; tmux paste-buffer"
}

# hot reload
unbind r
bind r source-file ~/.tmux.conf; display "Reloaded"

bind C-s choose-session

# to test (also https://github.com/christoomey/vim-tmux-navigator)
# smart pane switching with awareness of vim splits
# Uses tmux's built-in format matching (no shell spawn, ~8ms vs ~40-163ms)
# bind -n C-h if-shell -F '#{m:*vim*,#{pane_current_command}}' 'send-keys C-h' 'select-pane -L'
# bind -n C-j if-shell -F '#{m:*vim*,#{pane_current_command}}' 'send-keys C-j' 'select-pane -D'
# bind -n C-k if-shell -F '#{m:*vim*,#{pane_current_command}}' 'send-keys C-k' 'select-pane -U'
# bind -n C-l if-shell -F '#{m:*vim*,#{pane_current_command}}' 'send-keys C-l' 'select-pane -R'
